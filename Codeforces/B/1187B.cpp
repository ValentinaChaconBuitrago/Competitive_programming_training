#include<bits/stdc++.h>

using namespace std;

int main(){
	int n,num_friends;
	scanf("%d",&n);
	string word; scanf("%s",&word);
	scanf("%d",&num_friends);
	while(num_friends--){
		string name; scanf("%s",&name);
		for(int i=0;i<word.length();++i){

		}

	}
	return 0;
}